import { Info, ShieldCheck } from "lucide-react";
import type { ReactNode } from "react";
export function DemoNote(){return <div className="demo-note"><ShieldCheck className="size-3.5 text-success"/><span>Demo classroom</span><i/>Synthetic learning evidence</div>}
export function SectionHead({eyebrow,title,description,action}:{eyebrow?:string;title:string;description?:string;action?:ReactNode}){return <div className="mb-7 grid grid-cols-[minmax(0,1fr)_auto] items-end gap-4"><div className="min-w-0">{eyebrow&&<p className="section-kicker">{eyebrow}</p>}<h2 className="font-display text-3xl font-semibold leading-tight text-foreground md:text-4xl">{title}</h2>{description&&<p className="mt-2 max-w-2xl text-sm leading-relaxed text-muted-foreground">{description}</p>}</div>{action&&<div className="shrink-0">{action}</div>}</div>}
export function Stat({label,value,detail,tone="default"}:{label:string;value:string|number;detail?:string;tone?:"default"|"good"|"warn"}){return <div className="stat-signal" data-tone={tone}><span className="stat-signal-line"/><p className="text-xs font-semibold uppercase text-muted-foreground">{label}</p><p className="mt-2 font-mono text-3xl font-semibold tabular-nums">{value}</p>{detail&&<p className="mt-1 text-xs text-muted-foreground">{detail}</p>}</div>}
export function Insight({title,children}:{title:string;children:ReactNode}){return <div className="evidence-lens"><Info className="mt-0.5 size-4 shrink-0 text-info"/><div><p className="text-sm font-semibold">{title}</p><div className="mt-1 text-sm leading-relaxed text-muted-foreground">{children}</div></div></div>}
